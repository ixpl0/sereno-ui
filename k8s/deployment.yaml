apiVersion: apps/v1
kind: Deployment
metadata:
  name: sereno-portal
  labels:
    app: sereno-portal
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sereno-portal
  template:
    metadata:
      labels:
        app: sereno-portal
    spec:
      containers:
        - name: sereno-portal
          image: sereno-portal:latest
          ports:
            - containerPort: 3000
              protocol: TCP
          env:
            - name: HOST
              value: "0.0.0.0"
            - name: PORT
              value: "3000"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
